<div class="cat">
    <div class="categories">
        <% for(var i=0; i<categories.length; i++) {%>
            <div class="<%categories[i]%>" data-type="<%categories[i]%>">
                <button class="expansible"><%= categories[i].nom %></a></button>
                <ul class="scat" style ="display: none; list-style-type: none;">
                    <% for(var j=0; j<scategories.length; j++) {
                        if(scategories[j].nom == categories[i].nom){
                            const tmp = (scategories[j].snom);
                            const tmp2 = tmp.replace(/ /g,"_");
                            const link = tmp2.replace(/à/g,"a");
                            %>
                            <li class="scat" data-type="scategories" data-title="<%= scategories[j].snom %>"><a href="http://localhost:8080/rech_cat/<%=link%>"><%= scategories[j].snom %></a></li>
                        <%}%>
                    <% } %>
                </ul>
            </div>
        <% } %>
    </div>
</div>